# FileBridge
### A tool to transfer files fast with local network

## How to run:
1. pip install customtkinter ctklistbox
2. go build -o bin/serve go/serve/serve.go
3. go build -o bin/scan go/scan/scan.go